export interface WarehouseItem {
  id: number;
  name: string;
  category: string;
  categoryLabel: string;
  stock: number;
  unit: string;
  unitPrice: string;
  expiryDate: string;
  description: string;
}
